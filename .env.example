# PostgreSQL connection string (local dev default).
# Override in your local .env file (never commit real credentials).
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/unit_converter?schema=public"
